# NPM Configuration for Security

# Audit settings
audit=true
audit-level=moderate

# Lock file
package-lock=true

# Only install exact versions
save-exact=false

# Disable scripts from untrusted packages (security)
ignore-scripts=false

# Production mode configuration
# production=true  # Uncomment for production deployments
